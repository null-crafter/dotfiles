-- Monitor config. Owned by nwg-displays -- it overwrites this file on Apply.
-- Until then: every output at its preferred mode, auto position and scale.
hl.monitor({ output = "", mode = "preferred", position = "auto", scale = "auto" })
