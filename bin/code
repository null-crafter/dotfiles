#!/usr/bin/env sh

# I try to make vscode forks more ergonomic in this script
# If you use the official vscode, you do not need this script

CODE=codium
[ ! -z $WAYLAND_DISPLAY ] && OPTS=(--ozone-platform-hint=auto)

exec $CODE ${OPTS[@]} "$@"
